<?xml version="1.0"?>
<!DOCTYPE sect2 SYSTEM "/src/Docs/DocBookManuals/docbookx.dtd">
<sect2> 
	 <sect2info><date>Monday, April 05, 2004 13:40:11</date> 
	 </sect2info> 
	 <title>System.CacheTimeStamp</title> 
	 <para></para> 
	 <bridgehead renderas="sect3">Remarks</bridgehead> 
	 <para>Returns a date time representing the time the cache last had a time
		  stamp on it. The cache gets time stamped every time an entry in the cache is
		  changed. For example, if a table is added to the cache the cache timestamp is
		  not changed. However, when a table listed in the cache is changed or removed
		  the cache timestamp is updated.</para> 
	 <example> 
		  <title>Example</title> 
		  <programlisting>select TableDee add { CacheTimeStamp() Cache };</programlisting> 
	 </example> 
	 <formalpara> 
		  <title>See Also</title> 
		  <para> 
				<literallayout><ulink url="DDGCLICaching.html">Caching</ulink>
<ulink url="SLRSystem.UpdateTimeStamps.html">System.UpdateTimeStamps</ulink></literallayout></para> 
	 </formalpara> 
</sect2> 
